<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.caiocalado.studypool.RegisterActivity">

    <TextView android:text="@string/study_info"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:textSize="22dp"
        android:gravity="center_horizontal"
        android:textStyle="bold"
        android:textColor="#000"
        android:id="@+id/studyInfoText" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/helpText"
        android:id="@+id/helpButton"
        android:layout_below="@+id/studyInfoText"
        android:layout_alignEnd="@+id/studyInfoText"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/studyText"
        android:id="@+id/studyButton"
        android:layout_below="@+id/helpButton"
        android:layout_alignEnd="@+id/helpButton"
        android:layout_alignParentStart="true" />

</RelativeLayout>
